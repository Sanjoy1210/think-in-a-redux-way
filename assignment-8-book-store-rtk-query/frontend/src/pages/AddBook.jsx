import Layout from '@components/layout';
import AddBookPage from '@components/subPages/AddBookPage';

export default function AddBook() {
  return (
    <Layout>
      <AddBookPage />
    </Layout>
  );
}
