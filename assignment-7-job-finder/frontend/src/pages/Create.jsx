import Layout from '@components/layout';
import CreatePage from '@components/subPages/CreatePage';

export default function Create() {
  return (
    <Layout>
      <CreatePage />
    </Layout>
  );
}
