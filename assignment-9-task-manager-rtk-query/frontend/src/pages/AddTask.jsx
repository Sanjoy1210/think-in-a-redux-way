import Layout from '@components/layout';
import AddTaskPage from '@components/subPages/AddTaskPage';

export default function AddTask() {
  return (
    <Layout>
      <AddTaskPage />
    </Layout>
  );
}
